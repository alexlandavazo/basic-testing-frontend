name: Run Unit Composite
on:
  push:
    branches:
      - main 
jobs:
  run-unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Run Unit Tests
        uses: ./.github/workflows/reusable-workflow.yml@main